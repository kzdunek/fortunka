<%= simple_form_for(@friend) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :last_name %>
    <%= f.input :first_name %>
    <%= f.input :motto %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
